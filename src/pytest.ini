# pytest.ini

[pytest]
# Tell pytest to include the current directory (the project root)
# in the Python module search path. This allows 'tests/test_order_router.py'
# to correctly find 'main' (which is main.py) and 'app' modules.
pythonpath = .
asyncio_mode = auto